services:
  - type: web
    name: gapfinder-backend
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y tesseract-ocr poppler-utils
    startCommand: gunicorn app:app